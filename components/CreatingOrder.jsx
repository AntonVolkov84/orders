import { View, Text } from "react-native";
import React from "react";
import styled from "styled-components";

export default function CreatingOrder() {
  return (
    <View>
      <Text>CreatingOrder</Text>
    </View>
  );
}
